This directory stores the pretrained SYLPH checkpoint.

Run the following command from the repository root to download it:

    python checkpoint_utils.py

The checkpoint will be saved as:

    models/sylph/net_checkpoint.pkl
